<!DOCTYPE html>
<html lang="ko">
    <head>
        <%- include('header.ejs') %>
        <script
            type="text/javascript"
            src="https://unpkg.com/@zxing/browser@latest"
        ></script>
        <link rel="stylesheet" href="/stylesheets/register.css" />
    </head>
    <body>
        <%- include('nav.ejs') %>
        <div class="barcode-dialog" style="display: none">
            <div class="camera-dialog-container">
                <h2>학생증 뒷면의 바코드를 촬영해주세요</h2>
                <select
                    id="camera-list"
                    onchange="javascript:changeCamera(this.value)"
                ></select>
                <video id="camera"></video>
                <input
                    type="button"
                    class="btn btn-secondary"
                    onclick="javascript:onCancelClick()"
                    value="취소"
                />
            </div>
        </div>
        <div class="container">
            <h1>회원가입</h1>
            <form onsubmit="javascript:submitData(this)" action="#">
                <fieldset>
                    <div class="form-group">
                        <label for="studentIdInput">학생증 번호</label>
                        <div class="flex">
                            <input
                                type="text"
                                class="form-control"
                                id="studentIdInput"
                                placeholder="학생증 번호 입력(ex. S1000001)"
                                required
                                pattern="^S[0-9]{7}$"
                            />
                            <input
                                type="button"
                                class="btn btn-secondary"
                                value="카메라로 입력하기"
                                onclick="javascript:showDialog()"
                            />
                        </div>
                    </div>
                    <div class="form-group">
                        <label for="emailInput">이메일주소</label>
                        <input
                            type="email"
                            class="form-control"
                            id="emailInput"
                            aria-describedby="emailHelp"
                            placeholder="이메일 주소를 입력해주세요 (일정, 자리 변경 안내용)"
                            required
                        />
                    </div>
                    <div class="form-group">
                        <label for="usernameInput">아이디</label>
                        <input
                            type="text"
                            class="form-control"
                            id="usernameInput"
                            aria-describedby="idHelp"
                            placeholder="아이디를 입력해주세요"
                            required
                            maxlength="32"
                        />
                    </div>
                    <div class="form-group">
                        <label for="passwordInput">비밀번호</label>
                        <input
                            type="password"
                            class="form-control"
                            id="passwordInput"
                            placeholder="비밀번호를 입력해주세요"
                            required
                            maxlength="97"
                        />
                    </div>
                    <div class="form-group">
                        <label for="passwordCheckInput">비밀번호 확인</label>
                        <input
                            type="password"
                            class="form-control"
                            id="passwordCheckInput"
                            placeholder="비밀번호를 입력해주세요"
                            required
                        />
                    </div>
                    <button type="submit" class="btn btn-primary">
                        회원가입
                    </button>
                </fieldset>
            </form>
        </div>

        <script src="/js/register.js"></script>
    </body>
</html>
